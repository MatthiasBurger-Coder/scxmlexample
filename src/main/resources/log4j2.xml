<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <!-- Correlation-ID kommt aus MDC: %X{correlationId} -->
        <Property name="LOG_PATTERN">
            %d{HH:mm:ss.SSS} %-5p [%X{correlationId}] [%t] %c{36} - %msg%n
        </Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>
    </Appenders>

    <Loggers>
        <!-- dein Package auf DEBUG für AOP-Logs -->
        <Logger name="de.burger.it.scxmlexample" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
        </Logger>

        <Root level="debug">
            <AppenderRef ref="Console"/>
        </Root>
    </Loggers>
</Configuration>
